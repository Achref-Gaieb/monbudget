# Normalise line endings so the repository is identical on every platform.
* text=auto eol=lf

# Binary assets — never normalise.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
*.pdf binary

# Generated files — collapse in diffs and reviews.
package-lock.json linguist-generated=true -diff
